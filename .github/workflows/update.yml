name: Update npm Package

on:
  push:
    branches:
      - dev

jobs:
  update-package:
    runs-on: windows-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16
      
      - name: Install dependencies
        run: npm i
      
      - name: Prepare for deployment
        run: npm pack

      - name: Publish package
        run: |
          echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}" > ~/.npmrc
          npm version patch
          npm publish

      # - name: Update package version
      #   run: |
      #     $version = npm view story-book-test-package-git version
      #     $json = Get-Content -Raw -Path package.json | ConvertFrom-Json
      #     $json.version = $version
      #     $json | ConvertTo-Json -Depth 100 | Set-Content -Path package.json

      - name: Commit and push changes
        run: |
          git config --local user.name "Risini1998"
          git config --local user.email "risinipiyathma1998@gmal.com"
          git add .
          git commit -m "Update package version to $VERSION"
          git push

